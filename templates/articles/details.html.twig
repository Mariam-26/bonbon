{% extends 'base.html.twig' %}

{% block title %}
	{{article.title}}
{% endblock %}

{% block body %}
	<div container>
		<div class="my-5 bg-light">
			<div class="container">
				<h1 class="display-5">BIENVENUE !</h1>
				<p class="lead">Page d'accueil</p>
			</div>
		</div>
	</div>

	<div class="card-group container d-flex">
		<div class="row d-flex">
			<div class="col-lg-6 col-md-6 col-sm-12">
				<img src="{{article.image}}" alt="Illustration de l'article {{article.title}}" class="card-img-top img-fluid">
				<div class="card-body">
					<h3 class="card-title">{{article.title}}</h3>
					<h5>Catégorie :
						{{article.categories.name}}
					</h5>
					<p class="card-text">{{article.content|raw}}</p>
					<h6>Author :
						{{article.author}}
					</h6>
					<div class="my-1">
						<a href="{{ path('articles_retour') }}" class="btn btn-primary my-5">RETOUR</a>
					</div>
				</div>
			</div>
		</div>
		<div class=" card">
			<div class="">
				<h2>Liste des commentaires</h2>
			</div>
			<div class="">
				<h2>Ajouter un commentaire</h2>
				{{ form(form) }}
					{# {{ dump(form) }}  #}
					{# <div class="my-1">
						<a href="{{ path('articles_comments') }}" class="btn btn-primary my-5">RETOUR</a>
					</div>
					<button class="btn">{{ button_label|default('Save') }}</button> #}
					}

				</div>
			</div>
		</div>
		{# {{ dump(articles) }} #}
	{% endblock %}
